#include<stdio.h>
int main()
{
    int num1,num2,sum;
    printf("Enter numbers: ");
    scanf("%d%d",&num1,&num2);
    sum = num1 + num2;
    printf("Addition of two numbers: %d",sum);
    return 0;
}